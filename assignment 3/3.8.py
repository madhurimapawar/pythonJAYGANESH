#Explain why following code works without declaration ?

x=100


#The code works without declaration because Python is a dynamically typed language. In Python, you do not need to explicitly declare the type of a variable before assigning a value to it. When you assign a value to a variable (in this case, `x = 100`), Python automatically infers the type of the variable based on the value assigned.